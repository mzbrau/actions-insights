#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ -f "${SCRIPT_DIR}/scripts/init-history-repo.sh" ]]; then
  exec "${SCRIPT_DIR}/scripts/init-history-repo.sh" "$@"
fi

exec "${SCRIPT_DIR}/../../scripts/init-history-repo.sh" "$@"
