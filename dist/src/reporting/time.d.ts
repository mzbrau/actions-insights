/** Format an ISO timestamp for markdown output in UTC. */
export declare function formatUtcTimestamp(iso: string): string;
/** Use the latest test-result file mtime as the run completion time when available. */
export declare function resolveRunCompletedAt(sourceFiles: string[], fallback: string): string;
//# sourceMappingURL=time.d.ts.map